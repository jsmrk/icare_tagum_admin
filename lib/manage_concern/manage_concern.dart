import 'package:flutter/material.dart';

class ManageConcern extends StatelessWidget {
  const ManageConcern({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Manage Concern'),
      ),
    );
  }
}
