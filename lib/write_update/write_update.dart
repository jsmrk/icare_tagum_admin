import 'package:flutter/material.dart';

class WriteUpdate extends StatelessWidget {
  const WriteUpdate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Write Update'),
      ),
    );
  }
}
